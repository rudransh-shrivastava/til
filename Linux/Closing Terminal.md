You can actually close the terminal by just using this handy shortcut `Ctrl + d`

And, If you're using zsh / ohmyzsh `Ctrl + d` will show you command suggestions, this is different than tabbing because this doesn't actually complete the command.
